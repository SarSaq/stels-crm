# Supabase Management API (для скриптов/CI)
SUPABASE_ACCESS_TOKEN=
SUPABASE_DB_PASSWORD=

# Frontend (Next.js) — берутся из Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Только сервер (импорт/сервисные операции) — не коммитить реальное значение
SUPABASE_SERVICE_ROLE_KEY=
